name: Run Unit Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          # Unique per (workflow, job) so this job never races another workflow's
          # job over the same content-based setup-uv cache key.
          cache-suffix: ${{ github.workflow }}-${{ github.job }}

      - name: "Set up Python"
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxml2-dev libxslt-dev python3-dev build-essential

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Install Playwright Chromium Browser
        run: uv run playwright install chromium

      - name: Run Tests with Pytest
        run: uv run pytest --cov=src/oddsharvester --cov-report=xml:coverage.xml --cov-report=term

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v6
        with:
          fail_ci_if_error: true
          files: ./coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
