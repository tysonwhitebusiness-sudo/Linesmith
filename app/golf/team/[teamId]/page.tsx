'use client';

import { NoTeamPage } from '@/components/NoTeamPage';

/** Golf has no teams; the URL explains why and points to the players and schedule (R7). */
export default function GolfNoTeamPage() {
  return <NoTeamPage sport="golf" />;
}
