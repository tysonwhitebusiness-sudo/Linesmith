# Closeout — what is left after the unattended run (2026-09-23)

**All sixteen phases of `docs/design/unattended-run-2026-09-21.md` §2 are
built and deployed.** This file is the list of everything still standing
between that and "closed", written after a pass that found the closing itself
had been over-claimed.

**Read this with `docs/CURRENT.md`.** That file says where the work is; this
one says what finishing it costs. Delete this file when every box below is
ticked.

---

## STATUS — checkpoint 2026-09-23 ~19:50 UTC (resume here)

- **A1 answered: keep spotlights ungraded.** Q26 approved as built.
- **A2 answered: Q27 fine; Q28/Q29/Q30 investigated** — findings below,
  ONE decision still owed by the operator (the Charting Project licence).
- **B1 DONE.** `/kit` checked at 1440 and 400; all three pieces render in
  every state. Fixed on the way: a graded hit was announced as "Win".
- **B2 PARTLY DONE.** Swept all 10 sports at both widths. Fixed: the date
  field overflow (C6), NFL face 404s (namespaced ids), golf Movers 400s.
  1440 is now clean everywhere (1425px on every page).
  **STILL OPEN at 400: the five team-sport pages scroll to 435px** (was 620).
  Soccer, tennis and golf are 385. Next step: find the 35px (likely the date
  row: Today + Tomorrow + a 176px date field + two icon buttons > 400px) and
  fix it, then re-run the 400 sweep. Screenshots are in
  `docs/design/closeout-shots/` (untracked until B4 re-takes them).
- **B3, B4 not started.**
- **Off-list, fixed and DEPLOYED (`196f11f`):** `golfHistoryJob` was failing
  every 5 min in production on the Presidents Cup (a team event, same
  list-of-lists shape as the Ryder Cup). Recovered at 19:34 UTC, verified.

### A2 findings (for the operator)

- **Q28 WTA serve data:** ESPN has NONE for tennis (not even a US Open match).
  The only live source is Sackmann's **Match Charting Project** — 355 charted
  WTA matches in 2026 through the US Open QF (Sept 9), 327 players, full
  names, a Surface column. Same CC BY-NC-SA licence as TML, and its README is
  emphatic that attribution is required and use must be non-commercial.
  **Needs your licence call before ingest.** Hold/break need its point file.
  Also found: **TML's ATP feed stalled 2026-01-17** (`ongoing_tourneys.csv`
  is the Australian Open, Jan 18-21, and nothing after), so ATP 2026 serve
  data is not "thin until TML catches up" — it may not catch up. The Charting
  Project has ATP through 2026-05-21.
- **Attribution gap:** TML is also CC BY; the tennis cards credit it only
  inside a tooltip. Should be a visible source line.
- **Q29 stale surface:** no automatable source exists — ESPN carries no
  surface (now actually measured; the old code comment claimed it without
  checking), and tennis-data.co.uk answers bots with 403. Matching ESPN's
  tournament NAME to past seasons' surface gives an exact answer for 3 of 7
  current events; keep "current swing" as the fallback.
- **Q30 freezes:** could not test per-day first-tee freezing — this week is
  the Presidents Cup, which has no stroke-play tee sheet. Re-check next
  stroke-play week; `freezes=False` stays until then.

### Found in B2, logged not fixed (pre-existing, not breaking)

- **MLS crests for Seattle and Salt Lake 404** — `espnLogo('usa.1', abbr)`
  (since S1); soccer games carry no team id to key on. Falls back to text.
- **~1/3 of WTA headshots 404** on ESPN's CDN — already documented in
  `identity.ts` ("tennis 2/3"); the avatar falls back.

---

## A. Needs the operator — nothing can close without these

| # | what | why it needs a person | cost of the answer |
|---|---|---|---|
| **A1** | **Q26: should spotlights be graded?** `SPC`'s spec asks for "receipts for frozen spotlights, graded the next morning, like Specials". PY-A/PY-B built the opposite and F0's guard pins it in both languages | It is a product question, not a technical one. A Special is a book promo with a yes/no outcome; a research flag ("back in the lineup", "12 yards from 1,000") is a fact about today with nothing to settle | **Keep as built:** free. **Reverse:** a `grade_stat` and a hit rule for each of 36 spotlights, plus deleting the guard — roughly a phase |
| **A2** | **Q27–Q30: acknowledge four recorded decisions** — golf's two unbuilt live cards, WTA having no serve source at all, the 25-day-stale surface feed, and `RankingDef.freezes` | Each was taken without you under A6. None blocks code; all four change what the app claims | Reading five rows in `SIGNOFF-QUEUE.md` |
| ~~**A3**~~ | ~~Decide what happens to the other session's uncommitted odds-model refactor~~ | **Resolved 2026-09-23**: that session committed it as `af2ef13` and `f4a8373` ("Phase 0: root-cause fix + dead-code cleanup for the two-model system"). The tree is clean | — |

---

## B. Finish C8 properly — I over-claimed "Track C closed"

C8's own checklist has five items and three were done. These are the two and a
half that were not. **All of it is mine to do; none needs the operator.**

### B1 — `/kit` at 1440 and 400

Track C added three kit pieces and none has been looked at on the kit page.

1. Start the dev-verify server: `linesmith-dev-verify` on `:3001` (`/kit` is
   dev-only and does not exist on the prod build).
2. Render `/kit` at **1440** and **400** in Playwright (the built-in pane
   cannot draw Suspense pages — see `CURRENT.md`).
3. Check each piece in every state it has:
   - `DisclosureBar` — collapsed, open, and the one-time peek;
   - `PercentileCell` — a high percentile, a low one, and `percentile={null}`
     (which must print the value alone, no bar);
   - `ResultMark` — `hit`, `miss`, `dnp`, and `kind="dot"`.
4. If a piece is missing from `/kit`, add it there. The U-track rule is that
   the kit page shows every primitive in every state.

**Done when:** all three render correctly at both widths and any gap on the
kit page is filled.

### B2 — the full render sweep

C8 asks for **every sport** at 1440 and 400, fresh tabs, no console errors,
document width equal to viewport width, with screenshots in the closing
commit. What was actually done: MLB at both widths, tennis at 1440.

Sweep, in a fresh tab each: `/mlb`, `/nfl`, `/cfb`, `/nba`, `/nhl`,
`/soccer/epl`, `/soccer/mls`, `/tennis/atp`, `/tennis/wta`, `/golf`.

For each, record: the spotlight cards that rendered, console errors, and
`document.documentElement.scrollWidth`.

**Two things already known, so they are not findings:** the two pre-existing
signed-out 401s (`/api/picks`, `/api/watchlist`) appear on every page, and
`/mlb` overflows to 620px at a 400px viewport because of Scan's frozen table —
measured as unchanged by this run.

**Done when:** every sport renders at both widths with no NEW console error,
and the screenshots are attached to the closing commit.

### B3 — the 390px control-rows guard

C6.3 asks that the filter controls occupy **at most three rows at 390px**. It
was never automated: the repo had no browser driver when C6 shipped. **It has
Playwright now**, so the excuse has expired.

1. Add a Playwright-driven check (its own script, not `node --test`, which
   cannot drive a browser).
2. Measure the distinct `y` offsets of the control row's children at 390px.
3. Assert ≤ 3.

**Done when:** the check runs and passes, or it fails and C6's layout is fixed.

### B4 — re-close C8

Only after B1–B3: one commit, `C8: …`, with the screenshots, and update this
file plus `CURRENT.md`.

---

## C. Dated — cannot be done before these dates

| # | what | when | what to check |
|---|---|---|---|
| **C1** | **C5-UI sign-off** — built, never signed off | NFL Sunday **2026-09-27**, plus one MLB day graded by PY-A's code | The receipts card against real graded rows: the result bar, the last-N-slates bars, and the stat lines in "What happened" |
| **C2** | **The NFL grading gap** — `nfl-longest-reception` for 09-21 wrote a `__leader__` row and graded ZERO of its 11 ranked players, because `grade()` skips a row whose team's game has not landed in `player_game_history` | Same day, 2026-09-27 | If it repeats, NFL receipts never fill. The fix is in Python's grader, not the UI — do not "fix" it in the card |
| **C3** | **ATP serve data for the current season is thin.** TML's `2026.csv` held **137** matches against 2,945 for 2025 | Whenever TML catches up | `tennis-serve-return` leans on 2025. Re-run `tennis_stats.py ingest 2026 2026` and check the count moves |

---

## D. Carried backlog — outside this run, not created by it

- **NBA spotlights**, deferred under A6: needs a Python `'nba'` games loader
  (`load_sport_games` has no entry). Everything else for them is built.
- **C4 follow-ups**, both data additions to `lib/slate/marketMoves.ts`: Movers
  game-line rows show text matchups (no team ids on `ConsensusMover`), and
  "books moved" is a count (no per-book list on the row).
- **C3 follow-ups**: `matchup` typed but never populated; the rail's
  team/game/injury filters are not wired.
- **`SIGNOFF-QUEUE.md` Q0–Q25**, roughly eleven still `awaiting`. Q15 and
  `/diagnostics` need a signed-in session, which an agent cannot do.
- **M4** (promotion tests) and **M5** (prop baselines — needs approval).

---

## Order to work it

1. ~~A3~~ — resolved; the other session committed its work.
2. **B1 → B2 → B3 → B4.** Half a day, no dependencies, closes Track C for real.
3. **A1 and A2** whenever you get to them; A1 decides whether SPC reopens.
4. **C1/C2 on 2026-09-27.** Put it in the calendar — it is the only thing here
   with a deadline that passes.
5. **D** is ordinary backlog.
